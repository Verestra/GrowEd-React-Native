import * as React from 'react';
import { View, Text, Image } from 'react-native';
import styles from './Style';

function MyClass({ navigation}) {
    return (
        <View style={{paddingTop: 40}}>
            <Text>This is MyClass</Text>
        </View>
     );
     }

export default MyClass
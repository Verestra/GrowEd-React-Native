import * as React from 'react';
import { View, Text, Image } from 'react-native';
import styles from './Style';

function Activity({ navigation}) {
    return (
        <View style={{paddingTop: 40}}>
            <Text>This is Activity</Text>
        </View>
     );
     }

export default Activity